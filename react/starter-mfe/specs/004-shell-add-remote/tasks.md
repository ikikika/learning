# Tasks: Shell add-remote command

**Input**: Design documents from `/specs/004-shell-add-remote/`

**Prerequisites**: plan.md, spec.md (incl. analyze clarifications FR-019/020), research.md, data-model.md, contracts/, quickstart.md

**Tests**: Required by FR-020 / SC-003 / SC-006 — contract, unit, and compose/shell smokes included.

**Organization**: By user story (US1–US5). Regenerated after clarify pass resolving `/speckit-analyze` findings.

## Format: `[ID] [P?] [Story] Description`

- **[P]**: Parallelizable (different files, no incomplete deps)
- **[Story]**: `[US1]`…`[US5]` on story-phase tasks only
- Exact file paths in every description

---

## Phase 1: Setup (Shared Infrastructure)

**Purpose**: Package script and schema/docs surfaces

- [X] T001 Add `"add-remote": "node scripts/add-remote.mjs"` to `package.json` scripts
- [X] T002 [P] Document additive `remoteProps` in `specs/001-react-role-scaffold/contracts/role-metadata.schema.json` (align with `specs/004-shell-add-remote/contracts/role-metadata-remote-props.md`) and document per-remote `*_URL` keys in `.env.example`

---

## Phase 2: Foundational (Blocking Prerequisites)

**Purpose**: Shared helpers + webpack bake used by all stories

**⚠️ CRITICAL**: Blocks all user stories

- [X] T003 Extend `scripts/remotes-config.cjs` with `urlFromPort(port, host)`, `parsePropsJson(raw)`, `normalizeRemotePropsMap(map)`, and `writeLoadersGenerated(remotes, root)` reusing `generateLoadersSource`
- [X] T004 [P] Add `getRemoteProps(alias)` to `src/core/constants/remotes.ts` reading `__STARTER_REMOTE_PROPS__` (default `{}`; unknown/orphan alias → `{}`; **do not** add `src/app/remotes/remoteProps.ts`)
- [X] T005 [P] Bake `__STARTER_REMOTE_PROPS__` from `starter.role.json.remoteProps` in `config/webpack.common.js` DefinePlugin (non-shell builds → `{}`)
- [X] T006 Update `generateLoadersSource` header in `scripts/remotes-config.cjs` to “Generated by init / add-remote” and widen `RemoteModule` component props beyond only `embedded`

**Checkpoint**: Foundation ready

---

## Phase 3: User Story 1 — Register one running remote (Priority: P1) 🎯 MVP

**Goal**: Shell-only CLI registers one remote (meta + env + loaders); nav can load it

**Independent Test**: Empty `remotes[]` shell → `npm run add-remote -- --alias=… --port=…` (or `--url=…`) → restart → nav entry + panel load/fallback

### Tests for User Story 1

- [X] T007 [P] [US1] Contract tests in `tests/contract/add-remote-cli.test.mjs`: non-shell role gate (no writes), duplicate alias reject, missing/invalid `--url`/`--port`, invalid flag combos
- [X] T008 [P] [US1] Contract tests in `tests/contract/add-remote-writes.test.mjs`: success writes `starter.role.json` remotes[], `.env[urlEnv]`, and regenerates `src/app/remotes/loaders.generated.ts`

### Implementation for User Story 1

- [X] T009 [US1] Implement `scripts/add-remote.mjs` honoring full CLI contract in `specs/004-shell-add-remote/contracts/add-remote-cli.md` (`--alias`, `--url`|`--port`, `--name`, `--expose`, `--federation-name`, `--url-env`; shell-only; append remotes[]; write `.env`; regen loaders; print restart hint)
- [X] T010 [US1] Keep `scripts/init.mjs` remotes[]/loaders behavior compatible if shared writers are extracted (no change to init defaults for empty remotes)

**Checkpoint**: US1 complete — MVP registrable remote

---

## Phase 4: User Story 2 — Add several remotes one at a time (Priority: P1)

**Goal**: Second registration keeps the first; both appear in nav

**Independent Test**: add-remote A then B → both in `remotes[]` + loaders + shell nav; each selection loads its alias

### Tests for User Story 2

- [X] T011 [P] [US2] Extend `tests/contract/add-remote-writes.test.mjs`: two sequential successful adds leave both aliases in meta and loaders

### Implementation for User Story 2

- [X] T012 [US2] Ensure `scripts/add-remote.mjs` appends `remotes[]` without removing prior entries or unrelated `.env` keys; regenerates loaders for the **full** list
- [X] T013 [US2] Assert multi-slot listing via existing `REMOTE_SLOTS` + `src/features/demoShell/DemoShell.tsx` (fix only if multi-remote nav fails)

**Checkpoint**: US2 complete

---

## Phase 5: User Story 3 — Pass different props per remote (Priority: P1)

**Goal**: `remoteProps` per alias; LoadRemote merge with `embedded={true}`; sample shows `title` via in-remote context; no-props and orphan-props behaviors

**Independent Test**: Two aliases with different `title` values → each panel shows only its `demo-remote-host-title`; alias without props shows no host title; orphan props key ignored

### Tests for User Story 3

- [X] T014 [P] [US3] Unit test in `src/core/constants/remotes.ts` (or co-located test): `getRemoteProps` returns bag for known alias and `{}` for orphan/unknown
- [X] T015 [P] [US3] Unit test for LoadRemote props merge / `embedded` authority in `src/app/remotes/loadRemote.tsx` (or extracted helper test): bag `embedded` stripped; host forces `embedded={true}`
- [X] T016 [P] [US3] Contract tests in `tests/contract/add-remote-cli.test.mjs`: `--props` object written to `remoteProps[alias]`; invalid JSON / non-object fails with **no** file writes
- [X] T017 [P] [US3] Component test in `src/features/demoRemote/DemoRemoteRoute1.test.tsx`: shows `data-testid="demo-remote-host-title"` when context title set; absent when no title
- [X] T018 [P] [US3] Playwright (or compose) coverage in `tests/integration/compose.spec.ts`: two remotes / two titles — each selection shows only its title (FR-020c / SC-006)

### Implementation for User Story 3

- [X] T019 [US3] Extend `scripts/add-remote.mjs` with optional `--props` writing `starter.role.json.remoteProps[alias]`
- [X] T020 [US3] Update `src/app/remotes/loadRemote.tsx` to spread `getRemoteProps(alias)` onto lazy remote with `embedded={true}` winning
- [X] T021 [US3] Add host-props context provider in `src/app/FederatedRemoteApp.tsx` (and small `src/features/demoRemote/HostPropsContext.tsx` if needed) feeding mount `title` into remote tree (FR-019)
- [X] T022 [P] [US3] Update `src/features/demoRemote/DemoRemoteRoute1.tsx` (+ styles) to read context and render visible host title when non-empty
- [X] T023 [US3] Ensure no-props mount path: registered remote without `remoteProps` entry shows Route 1 without host title element (FR-013)

**Checkpoint**: US3 complete — distinct props observable

---

## Phase 6: User Story 4 — Remote internal routes when composed (Priority: P1)

**Goal**: With or without host props, MemoryRouter routes work; Route 2 reachable in automated smoke (required)

**Independent Test**: Open remote in shell → Route 1 visible → navigate to Route 2 → Route 2 content visible; shell stable

### Tests for User Story 4

- [X] T024 [US4] Extend `tests/integration/compose.spec.ts` (and/or `tests/integration/shell.spec.ts`): after opening remote panel, assert `demo-remote-route-1`, then navigate to Route 2 and assert `demo-remote-route-2` (FR-020a/b / SC-003) — **not optional**

### Implementation for User Story 4

- [X] T025 [US4] Keep `src/app/FederatedRemoteApp.tsx` + `src/app/routes/remoteRoutes.tsx` MemoryRouter/`embeddedRoutes` working when host props/context wrap the tree
- [X] T026 [P] [US4] Ensure `src/features/demoRemote/DemoRemoteRoute2.tsx` remains usable without requiring `title` (add a documented in-remote nav control or test-only navigation helper if smoke cannot reach `/route-2` otherwise)

**Checkpoint**: US4 complete

---

## Phase 7: User Story 5 — Standalone and remote roles unchanged (Priority: P1)

**Goal**: Non-shell add-remote fails cleanly; standalone/remote own-app flows unchanged

**Independent Test**: add-remote on remote/standalone → non-zero, no writes; standalone + remote-standalone e2e still pass without requiring host title

### Tests for User Story 5

- [X] T027 [P] [US5] Confirm non-shell no-mutation covered once in `tests/contract/add-remote-cli.test.mjs` (single suite; avoid duplicate-only tasks)
- [X] T028 [P] [US5] Run/adjust `tests/integration/standalone.spec.ts` and `tests/integration/remote-standalone.spec.ts` so they pass without `demo-remote-host-title`

### Implementation for User Story 5

- [X] T029 [US5] Enforce shell-only writes in `scripts/add-remote.mjs` with clear stderr
- [X] T030 [US5] Leave `src/app/App.tsx`, `src/app/routes/standaloneRoutes.tsx`, and `src/app/routes/shellRoutes.tsx` behaviorally unchanged (no drive-by edits)
- [X] T031 [US5] Own-app path: without shell props, `DemoRemoteRoute1` keeps prior copy (no host title element)

**Checkpoint**: US5 complete

---

## Phase 8: Polish & Cross-Cutting Concerns

**Purpose**: Docs, orphan-props check, quickstart, responsive spot-check

- [X] T032 [P] Update `README.md` for `npm run add-remote` (full flags), optional `--props`, hand-edit `remoteProps`, restart note (FR-010 / FR-016)
- [X] T033 [P] Update `AGENTS.md` and `docs/coding-conventions.md` for shell-only add-remote + per-alias props
- [X] T034 [P] Contract or unit coverage for orphan `remoteProps` key ignored at runtime in `tests/contract/add-remote-cli.test.mjs` or `src/core/constants/remotes.ts` test (FR-017 / FR-020d)
- [X] T035 Run `specs/004-shell-add-remote/quickstart.md` validation path (contract tests + compose/title/route-2 smokes)
- [X] T036 [P] Spot-check shell multi-remote nav at ~375px width remains usable

---

## Dependencies & Execution Order

### Phase Dependencies

- Setup → Foundational → US1 (MVP) → US2
- US3 needs Foundational + US1 CLI base (T009) before `--props` (T019)
- US4 after US3 context wrap on FederatedRemoteApp
- US5 role gate with US1; sample “no title” after US3 UI
- Polish after desired stories

### User Story Dependencies

| Story | Depends on |
|-------|------------|
| US1 | Foundational |
| US2 | US1 CLI append |
| US3 | Foundational + US1 |
| US4 | US3 FederatedRemoteApp/context |
| US5 | US1 gate + US3 sample UI |

### Parallel Opportunities

- T001 ∥ T002
- T004 ∥ T005 (after T003 APIs known)
- T007 ∥ T008
- T014 ∥ T015 ∥ T016 ∥ T017
- T022 ∥ T026 (different files)
- T027 ∥ T028
- T032 ∥ T033 ∥ T034 ∥ T036

---

## Parallel Example: User Story 3

```bash
# Tests in parallel:
Task: "getRemoteProps unit test in remotes.ts"
Task: "LoadRemote embedded-authority unit test"
Task: "add-remote --props contract tests"
Task: "DemoRemoteRoute1 title component test"
# Then implement CLI props + LoadRemote + context + Route1 UI
```

---

## Implementation Strategy

### MVP First (US1 only)

1. Phase 1–2  
2. Phase 3 (T007–T010)  
3. **STOP** — validate one remote registers and loads  

### Incremental Delivery

US1 → US2 → US3 (props + titles) → US4 (Route 2 smoke) → US5 → Polish (docs + orphan + quickstart)

### Parallel Team Strategy

- Shared: Setup + Foundational  
- A: US1/US2 CLI  
- B: US3 LoadRemote + context + sample UI  
- C: US4/US5 smokes + polish  

---

## Notes

- Suggested MVP: **T001–T010** (US1)
- No `src/app/remotes/remoteProps.ts` — use `getRemoteProps` in `remotes.ts`
- FR-020 verification mapped to T018, T024, T034
- Format: all tasks use `- [ ] Txxx` with paths and story labels where required
