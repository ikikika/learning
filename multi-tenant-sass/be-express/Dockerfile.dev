FROM node:20-alpine

WORKDIR /usr/src/app

# Install dependencies (include dev for development)
COPY package.json package-lock.json* ./
RUN npm ci

# Copy source
COPY . ./

EXPOSE 3000

# Development entry: ts-node-dev watches and restarts on changes
CMD ["npm", "run", "dev"]
